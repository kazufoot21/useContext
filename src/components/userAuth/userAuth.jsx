import React from "react";
import { AuhtButton } from "./AuthButton";
import { AuthDisplay } from "./AuthDisplay";

export const UserAuth = (props) => {
  return (
    <>
      <h4>user admission</h4>
      <AuhtButton />
      <AuthDisplay />
    </>
  );
};
